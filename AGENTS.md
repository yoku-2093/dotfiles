## 応答と作業分担

- 対話の即応を最優先する。ユーザーの質問には5秒以内に返す。調べる必要があっても、
  まず現時点で分かっていることと方針を1レス返してから動く。
- 数分かかる作業（実装、検証、build、独立 review、commit、push）は subagent へ委譲し、
  main agent は自分では抱えない。main agent が直接叩くのは数秒で終わる調査コマンドだけ。
- 委譲する prompt には、目的・現状（変更済みファイルと済んだ検証）・実行すべき検証
  コマンド・禁止事項・報告形式を明記する。subagent は background で走らせ、完了通知を待つ。
- `sleep` や長い timeout で待たない。CI や外部処理の完了待ちは、現状を報告してから
  background 実行か次のターンでの単発確認に回す。
- 進行中の subagent の結果を推測して報告しない。「まだ走っている」と述べる。
- 質問への回答は結論から書く。フィールド名やコマンドなど、確認済みの具体値で答え、
  前置きと選択肢の羅列をしない。
